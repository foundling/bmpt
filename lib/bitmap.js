var Bitmap = function(src_path,transformations){
    this.src_file = src_file;   
    this.transformations = transformations;
    this.destination_file = destination_file;
    this.header_data = [];
    this.pallete_data = [];
    this.bitmap_data = [];

    // initialization verification code

};

module.exports = {
    Bitmap: Bitmap,
}
